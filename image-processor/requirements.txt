# 코어 (gemini-edit 파이프라인 + 워커)
google-genai>=1.0.0
redis>=5.0
boto3>=1.34
requests>=2.31
pillow>=10.0
python-dotenv>=1.0

# 임베딩 (WORKER_EMBED_ENABLED=0이면 미사용 — torch는 베이스 이미지에 포함)
open_clip_torch>=2.24
sentence-transformers>=3.0
numpy>=1.26
