# 레거시 indexer(ETRI 패션 코디 데이터셋 적재) 의존성.
# 공용 이미지 임베더(util/embedder.py) 의존성은 util/requirements.txt에 있다.
-r ../util/requirements.txt

# Qdrant 적재
qdrant-client>=1.9
# S3 원본 데이터
boto3>=1.34
# 이미지 디코딩
Pillow>=10.0
python-dotenv>=1.0
tqdm>=4.66
